export interface IProps {
  listname: string;
  context: any;
  resturl: string;
  itemcount: number;
  assetfolderurl: string;
}

export interface IPropsInsert{
  description: string;
  context: any;
  siteurl: string;
}